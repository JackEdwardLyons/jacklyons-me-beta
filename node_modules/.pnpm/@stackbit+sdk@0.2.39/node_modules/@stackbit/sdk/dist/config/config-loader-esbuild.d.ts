import { YamlConfig } from './config-types';
import { ConfigLoadError } from './config-errors';
export declare type LoadStackbitConfigResult = {
    config: YamlConfig;
    error: null;
} | {
    config: null;
    error: ConfigLoadError;
};
export declare type StopConfigWatch = {
    stop?: () => void;
};
export declare function loadStackbitConfigFromJs({ configPath, watch, callback, logger, outDir }: {
    configPath: string;
    watch?: boolean;
    callback?: (result: LoadStackbitConfigResult) => void;
    logger?: any;
    outDir?: string;
}): Promise<LoadStackbitConfigResult & StopConfigWatch>;
//# sourceMappingURL=config-loader-esbuild.d.ts.map